{
  "name": "wk-vocab-api",
  "version": "0.1.0",
  "description": "Backing API for wk-vocab-review-ik userscript — coalesces ImmersionKit, DuckDuckGo, and Google TTS behind a single pre-warmed endpoint.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "test": "bun test",
    "typecheck": "bun tsc --noEmit"
  },
  "dependencies": {
    "@hono/zod-openapi": "^1.4.0",
    "@scalar/hono-api-reference": "^0.10.19",
    "hono": "^4.6.12",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/bun": "^1.1.14",
    "typescript": "^5.6.3"
  }
}
