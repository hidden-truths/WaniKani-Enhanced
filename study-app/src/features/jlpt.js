// Thin re-export so main.js + cloud.js import the 合格 JLPT tab exactly like the other
// feature packages (`features/jlpt.js` → the features/jlpt/ directory's index barrel).
export * from './jlpt/index.js';
