{
  "name": "study-app",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "日常日本語 — standalone Japanese trainer (Vite). Talks to the wk-enhanced-api backend over HTTP.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "happy-dom": "^16.7.2",
    "vite": "^7.0.0",
    "vitest": "^3.0.0"
  }
}
